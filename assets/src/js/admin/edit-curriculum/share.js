/**
 * Share
 */
let elEditCurriculum;
let elCurriculumSections;
let lpUtils;
const setVariables = ( variables ) => {
	( { elEditCurriculum, elCurriculumSections, lpUtils } = variables );
};

export { setVariables, elEditCurriculum, elCurriculumSections, lpUtils };
